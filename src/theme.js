import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1DB954', // Spotify 绿色
    },
    secondary: {
      main: '#191414', // Spotify 黑色
    },
  },
});

export default theme;